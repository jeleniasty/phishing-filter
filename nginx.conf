upstream phishing_filter {
    server phishing-filter:8080;
}

server {
    listen 8080;
    location / {
        proxy_pass http://phishing_filter;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}